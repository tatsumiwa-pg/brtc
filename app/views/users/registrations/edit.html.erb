<div class="whole">
  <div class="bg">
    <div class="content">
      <%= link_to '一覧へ戻る', root_path, class:"back-btn" %>
      <h1 class="message">Edit User - ユーザー情報編集 -</h2>

      <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
        <%= render "users/shared/error_messages", resource: f.object %>

        <div class="form-group">
          <div class='form-text-wrap'>
            <label class="form-text">ニックネーム -Nickname- </label>
          </div>
          <%= f.text_area :nickname, class:"input-default", id:"nickname", placeholder:"例) furima太郎", maxlength:"40", autofocus: true %>
        </div>

        <div class="form-group">
          <div class='form-text-wrap'>
            <label class="form-text">メールアドレス -Email-</label>
          </div>
          <%= f.email_field :email, class:"input-default", id:"email" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="form-group">
          <div class='form-text-wrap'>
            <label class="form-text">パスワード -New Password-<br>（変更したくない場合はカラのまま進んでください）</label>

          </div>
          <%= f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
          <p class='info-text'>※英字と数字の両方を含めて設定してください</p>
        </div>

        <div class="form-group">
          <div class='form-text-wrap'>
            <label class="form-text">パスワード(確認)</label>
            <span class="indispensable">新しいパスワードを設定する場合は必須</span>
          </div>
          <%= f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
        </div>

        <div class="form-group">
          <div class='form-text-wrap'>
            <label class="form-text">Password - 変更を保存するには、パスワードの入力が必要です -</label>
            <span class="indispensable">必須</span>
          </div>
          <%= f.password_field :current_password, autocomplete: "current-password", class:"input-default", id:"current-password", placeholder:'今のパスワード' %>
        </div>

        <div class="submit">
          <h4>Edit</h4>
          <%= f.submit '変更する', class:"submit-btn" %>
        </div>
      <% end %>

      <br>

      <h3>退会をご希望の方は以下から</h3>
      <div class="cancel-account-wrap">

        <h4>退会しますか？</h4>
        <p>※注意！ あなたのアカウント情報は削除されますが、投稿したコンテンツはお名前以外残ります</P>
        <%= link_to '退会する', user_registration_path, method: :delete, data: { confirm: "Are you sure?" }, class:"cancel-btn", id:"cancel_btn" %>
      </div>
      <%= link_to '一覧へ戻る', root_path, class:"back-btn" %>
    </div>
  </div>
</div>